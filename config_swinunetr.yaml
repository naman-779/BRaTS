# BRaTS 2024 Post-Treatment Glioma Segmentation — SwinUNETR (Model 2/3)

# --- Paths ---
data_root: "/Users/namantaneja/Library/Mobile Documents/com~apple~CloudDocs/Desktop/My Mac/Work/College/Research/implementBRaTS/Brats GLI"
train_dir: "training_data1_v2"
val_dir: "validation_data"
output_dir: "./runs/swinunetr"
checkpoint_dir: "./checkpoints/swinunetr"

# --- Data ---
modalities: ["t1n", "t1c", "t2w", "t2f"]
num_classes: 5           # 0=BG, 1=NETC, 2=SNFH, 3=ET, 4=RC
num_input_channels: 4
val_split: 0.1
data_fraction: 0.03      # Use ~40 cases for quick baseline
seed: 42

# --- Model ---
model:
  name: "SwinUNETR"
  spatial_dims: 3
  in_channels: 4
  out_channels: 5
  feature_size: 24
  drop_rate: 0.0
  attn_drop_rate: 0.0
  use_checkpoint: false

# --- Training ---
training:
  roi_size: [96, 96, 96]
  batch_size: 1
  num_workers: 0
  max_epochs: 10
  val_interval: 5
  amp: false
  cache_rate: 0.0

# --- Loss ---
loss:
  softmax: true
  to_onehot_y: true
  include_background: false
  lambda_dice: 1.0
  lambda_ce: 1.0

# --- Optimizer ---
optimizer:
  lr: 1.0e-4
  weight_decay: 1.0e-5

# --- Scheduler ---
scheduler:
  warmup_epochs: 10

# --- Inference ---
inference:
  roi_size: [96, 96, 96]
  sw_batch_size: 1
  overlap: 0.5
  mode: "gaussian"
